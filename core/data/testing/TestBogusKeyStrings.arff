%
% ARFF file for testing parser (not sparse data)
% This file contains a variety of incorrect things in the
% "keywords" but is parsed w/o warning
%

% @ruth isn't anything - is it flagged?
@ruth LCCvsLCSH This doesn't break a thing

% No relation defined - seems fine.

% Put in some extra stuff after the strings
@attribute LCC STRING  ABC
@attribute LCSH STRING DEF

% Picked up as data even though should be comment.
%@data
AG5, 'Str'
data, test
ad3, hi
